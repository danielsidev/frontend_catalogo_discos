# Catálogo de Discos - Frontend

> Aplicação de catálogo de discos com Vue.js

## Build Setup

``` bash
# Instale as dependências do projeto
sudo npm install

# Irá rodar a aplicação em localhost:8080
sudo npm run dev

```
